{
  "name": "flagstatus",
  "private": true,
  "workspaces": [
    "backend",
    "app"
  ],
  "scripts": {
    "build": "npm run build:web --workspace=app",
    "start": "npm start --workspace=backend",
    "dev": "npm run dev --workspace=backend"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
